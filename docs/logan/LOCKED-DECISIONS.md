# Sensei Desk — locked decisions (Log / Logan)

Updated: Wed Sep 23 evening. Andy = Body. Logan = Eyes+Brain.

## Tutoring (non-negotiable — research-backed)

1. **First wrong step only** — verify, then tutor (Daheim / verify-then-generate).
2. **Stay quiet after detect** — no auto-lecture every frame.
3. **Hint on demand** — one Socratic Q aimed at that step; never dump the answer (Razzaq on-demand > auto-hints).
4. **Check** = did they fix it? binary; no solution dump.
5. **No spoon-feed / no Photomath path** — opposite of Fri story.
6. **No SYSTEM_PROMPT chase** as main bet — measure Lane A; Jev/FT only if numbers fail.
7. **ASR voice-in OFF for Fri** — taps only (demo ASR failed). **TTS audio-out OK** if short and non-leaking.

## Fri plant

- `physics/basic/medium` — planted `v_y = 20` (vector never resolved).
- Fix → `20 sin(60)` path ≈ 3.54 s total.
- Control: `good_1`. Algebra demoted to OCR warm-up only.

## Body UX you want (arm → paper → phone + audio)

**Long-term intent:** arm auto-aims / focuses down on paper → after assess, UI switches to phone screen with short spoken Hint/Check feedback.

**Fri discipline:**
- If Andy’s arm auto-focus isn’t solid by Thu noon → **fixed DJI overhead** (already proven in Drive demo). Do not bet Fri on unproven pan-tilt.
- Phone screen switch + **short TTS** after Hint/Check is good Fri polish *if already in app*; do not block Eyes+Brain measure on building new screen choreography.
- Audio = **tutor speak-out** (TTS), not student speak-in. Hint text still must not leak `v_y = 17.32` / `3.54 s`.

## Lanes (unchanged priority)

| Lane | Job | When |
|------|-----|------|
| A Measure | `eval_brain` on thinking VL; physics/medium first | Key lands |
| B Body | Arm focus + presets; Fri fallback DJI | Andy tonight–Thu |
| C Jev | Typed mistake? gate | Only if A soft/slow |
| D FT | Samples LoRA | Only if A fails catch/FA |
| E Prompt | HOLD | — |

## Apply now (no Spark)

- Print/hand plant `bad_1`; rehearse quiet→Hint→Check with Demo Coach scorecard.
- Capture hygiene (Scout): square-on, light, big dark digits.
- Andy ping still needs key + “arm auto-focus ready by Thu? else DJI.”

## Explicitly not Fri

- Continuous vision chat (“what do you see?”)
- Voice-in tutoring
- Full Pix2Text/OATutor integration (post-Fri stack sketch only)
- Building a vectors FBD OSS stack from scratch this week
