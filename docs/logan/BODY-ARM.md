# Body thread — arm → paper → phone + audio

## Intent
Arm auto-focuses / aims camera down on paper. After assess, UI switches to phone screen with short spoken Hint/Check feedback (TTS out).

## Fri discipline
- If arm auto-focus not solid by Thu noon → **fixed DJI overhead** (Drive demo proven).
- Screen switch + short TTS after Hint/Check = polish if already in app; not a blocker for Eyes+Brain measure.
- Audio = tutor speak-out only. Voice-in (ASR) OFF for Fri.
- Hint TTS must not leak correct `v_y` / final time.

## Owner
Andy (Body). Logan tracks via Log. Eyes+Brain does not own pan-tilt firmware this week.
