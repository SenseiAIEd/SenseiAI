# Andy ping — paste-ready Discord DM

Copy everything below the line into Discord → Andy. Send once; do not chase mid-demo.

---

Andy — Fri noon Eyes+Brain needs two things from you before Thu noon:

**1) Spark access (blocker for Lane A)**
- `SENSEI_KEY` (funnel / console)
- SSH or console path onto the Spark (same box Log already funnel-reaches)
- Current LLM block from `sensei.env`: `SENSEI_LLM_URL`, `SENSEI_LLM_MODEL`, `SENSEI_LLM_KEY`
- Confirm **thinking** VL is loaded (`qwen3-vl-30b-a3b-thinking` preferred), not instruct GGUF

**2) Body arm vs DJI (Thu noon hard cut)**
- Is arm auto-focus on paper solid by **Thu noon**?
- If not → we lock **fixed DJI overhead** for Fri (Drive demo path). Do not bet Fri on unproven pan-tilt.

**3) Quick confirm if already in the app (polish only — not a blocker)**
- After Hint/Check: does phone already switch to screen + short TTS speak-out?
- If yes, keep it (non-leaking only — must not say 17.32 / 3.54).
- If no, we skip polish; Eyes+Brain measure still ships.

Fri plant locked: `physics/basic/medium` (`v_y = 20`). Voice-**in** OFF; taps only.

— Logan / Log
